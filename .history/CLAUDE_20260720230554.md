# San Pablo City AI Citizen Assistant - System Instructions

## Project Context
You are an expert Frontend AI Engineer building a citizen services web app for San Pablo City (MySanPablo) — a digital gateway for city announcements, emergency hotlines, office directory, and issue reporting. The app must be fast, highly accessible (WCAG 2.1 AA), and resilient under poor mobile data conditions.

## Tech Stack
- **Framework:** React.js + Vite
- **Language:** JavaScript (JSX)
- **Forms & Validation:** React Hook Form + Zod (never use uncontrolled browser inputs or plain useState-based manual validation)
- **UI & Styling:** Tailwind CSS, Lucide React (for icons)
- **Testing:** Vitest + React Testing Library

## Core Coding Principles & Rules

### 1. Robust Form Validation (Strict Rule)
- All user inputs and citizen reports MUST be strictly validated client-side using React Hook Form paired with a declarative Zod schema.
- Show specific, human-readable error messages immediately under the failing input field.
- No form may ship without a corresponding Zod schema — a component using raw `useState` for validation fails review.

### 2. Accessibility & Mobile-First (WCAG 2.1 AA)
- Every interactive element (inputs, buttons, select menus) must have an explicit and descriptive `aria-label` or `aria-describedby`.
- All custom components must support full keyboard navigation (correct tab-indexing) — verified by tabbing through the form without a mouse.
- Color contrast ratios must meet at least WCAG AA standards (4.5:1 for normal text, 3:1 for large text).
- Every validation error must be linked to its input via `aria-describedby` so screen readers announce it — a visual-only error message fails review.

### 3. Clean Code & Architecture
- Components use `.jsx` files with functional components only — no class components, no `.js` files for anything rendering JSX.
- Hook names must state what they return or do (e.g. `useReportForm`, `useSubmitStatus`) — a hook named `useData` or `useStuff` fails review.
- Every component file must have a colocated test file (e.g. `ReportIssueForm.jsx` + `ReportIssueForm.test.jsx`) — a component with no test file fails review.
- Components live under `/src/components`, one component per file, no multi-component files.